apiVersion: v1
kind: Service
metadata:
  name: ai-codeguardian-service
  namespace: ai-codeguardian
  labels:
    app: ai-codeguardian-backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: ai-codeguardian-backend
